# 구글 코랩 호환성 검토 정리

예제 노트북(`notebooks/chNN/NN-NN_example.ipynb`)과 `code_reference/`를 대상으로,
**로컬 / 코랩(특히 구글 드라이브 백엔드) 양쪽에서 깃허브에서 당겨와 바로 실습 가능한가**를
검토한 결과를 정리한 문서. (작성일 2026-06-22)

---

## 한 줄 요약

코랩 자체 호환(한글 폰트 자동 설치·device 자동 선택·GPU 안내)은 잘 갖춰져 있다.
**실제 블로커는 `../../` 상대경로 한 가지**이며, 여기에 **TPU 런타임 자동선택 footgun**과
**드라이브를 작업 디스크로 쓸 때의 성능 저하**가 더해진다. 첫 셀에 **코랩 자동 감지 부트스트랩**을
넣고(아래 권장 구성), `get_device()`의 XLA 분기를 손보면 공통 구성이 완성된다.

---

## 우선순위 한눈에

| 순위 | 이슈 | 영향 | 조치 |
|---|---|---|---|
| 🔴1 | `../../` 상대경로(코랩 CWD=`/content`) | code_reference·data 전부 못 찾음 → 실행 불가 | 부트스트랩 셀 추가 |
| 🔴2 | README ↔ 노트북 불일치(git clone 안내 누락·`%cd` 누락) | 안내대로 해도 실패 | 표준 셀로 통일 |
| 🔴3 | TPU 런타임 → `get_device()` XLA 자동선택 | 에러 없이 멈춤/극저속 | XLA 분기 제거·경고 |
| 🟡4 | `!pip install accelerate>=1.1.0` | 셸 `>` 리다이렉션 버그 | 따옴표 |
| 🟡5 | 드라이브를 작업 디스크로 사용 | 다운로드·체크포인트 저속, 쿼터 | clone은 `/content`로 |
| 🟡6 | 한글 폰트 코랩 첫 실행 | 첫 그림 □, 재시작 필요 | 1회 재시작 안내 |
| 🟢7 | 임시 저장 소실 / 대용량 다운로드 | 실행엔 무방, 보존 시 주의 | 안내 |

---

## 1. 🔴 `../../` 상대경로 — 핵심 블로커

모든 노트북 셋업 셀 공통 패턴:

```python
sys.path.append('../../')
from code_reference import common
from code_reference import visualize as viz
```

그리고 데이터도 상대경로로 읽는다:
`../../data/ch3_spiral_data.csv`, `../../data/wonderful_wizard_of_oz.txt`,
`../../data/cat.jpg`, ch11 `../../data/Tuebingen_Neckarfront.jpg`·`starry_night.jpg`,
torchvision 다운로드 root `../../downloads/`.

**문제**: 코랩은 노트북을 어디서 열든 작업 디렉터리(CWD)가 항상 `/content`다.
- `../../` → `/content/../..` = `/` → `code_reference` 없음 → **`ModuleNotFoundError`**
- `../../data/...` → `/data/...` → **`FileNotFoundError`**

로컬(VS Code Jupyter·JupyterLab)은 `CWD=노트북 폴더`라 `../../`가 정확히 `repo_root`를
가리키므로 잘 동작한다. **즉 깨지는 곳은 코랩뿐.**

## 2. 🔴 구글 드라이브 백엔드 특이점

드라이브에 저장한 노트북을 코랩에서 열어도 **CWD는 드라이브 폴더가 아니라 `/content`**다.
드라이브에 올려 두는 것만으로는 `../../`가 절대 맞지 않으며,
`drive.mount` 후 `%cd`로 해당 챕터 폴더까지 들어가야만 경로가 성립한다.
**현재 어떤 노트북도 mount/`%cd`를 하지 않는다.**

## 3. 🔴 README ↔ 노트북 불일치

`README.md`(182–192행)는 "각 노트북 첫 셀에 주석 처리된 `!git clone`이 있고, 코랩에서 풀고
한 번 실행하면 이후 로컬·코랩 동일 동작"이라고 안내한다. 그러나

- **실제 노트북 첫 셀에는 `# !pip install …`만 있고 `!git clone`·`%cd`·`drive.mount`가 없다.**
- git clone만 해도 부족하다 — 클론 후에도 CWD=`/content`라 `%cd <repo>/repo_root/notebooks/chNN`이
  필요하다. README 설명 자체도 `%cd` 단계가 빠져 있어 그대로 따라 하면 실패한다.

## 4. 🔴 TPU 런타임 → XLA 자동선택 (footgun)

`code_reference/common.py`의 `get_device()`는 `cuda → mps → xla → cpu` 순으로 선택한다.
코랩에서 **TPU 런타임**을 고르면:

- TPU 런타임엔 `torch_xla`가 설치돼 있어 → `xm.xla_device()` 성공 → `get_device()`가 **XLA 장치 반환**.
- 그런데 노트북 학습 루프는 전부 **일반 eager 파이토치**(`.to(device)`, `loss.backward()`,
  `optimizer.step()`, 표준 `DataLoader`)다. XLA/TPU는 **지연 실행**이라
  `xm.optimizer_step()`·`xm.mark_step()`로 실행 경계를 찍고 `MpDeviceLoader`로 데이터를 먹여야 한다.
- 결과: 표준 루프 + 매 스텝 `loss.item()`(강제 동기) → **에러 없이 극심한 재컴파일/지연 또는 멈춘 듯**.
  `report_gpu_memory()`·`clear_gpu_cache()`(=`torch.cuda.*`)는 XLA에서 no-op.

→ TPU를 고른 독자는 "느리거나 멈춤"을 겪고 원인 안내가 없다.
이것이 "DataLoader ↔ TPU 충돌"의 실체(표준 DataLoader+루프가 XLA 래퍼 없이는 TPU에 안 맞음).

**권장(택1)**
- `get_device()`에서 **XLA 분기 제거** → TPU여도 CPU로 폴백(느려도 정확). 입문서엔 가장 안전.
- 또는 XLA 감지 시 **경고 후 CPU 반환**.
- README 코랩 절에 **"TPU 런타임 금지, GPU(T4)/CPU만"** 명시.

## 5. 🟡 DataLoader num_workers — 현재 안전

전 노트북의 `DataLoader`가 **모두 `num_workers=0`**(ch08·ch11·ch13-01 명시적 0, 나머지는
기본값 0)이다. 따라서 코랩에서 흔한
- `DataLoader worker killed: Bus error`(`/dev/shm` 공유메모리 고갈)
- fork 워커의 CUDA 재초기화 충돌

은 **현재 구성에서 발생하지 않는다.** (잘 설계됨)

> 주의: 독자가 속도용으로 `num_workers`를 올리면 코랩 무료 인스턴스의 작은 `/dev/shm`에서
> Bus error가 날 수 있다. 도전 문제(Flickr8k 전체·Clotho 등)에 "코랩에선 0~2로 제한" 한 줄 권장.

## 6. 🟡 구글 드라이브 마운트(작업 디스크) 성능

드라이브 마운트는 **FUSE 네트워크 파일시스템**이라 작은 파일 다수·랜덤 액세스에 지연이 크고,
일일 I/O 쿼터가 있으며 대용량 쓰기가 느리다. 이 노트북들에서 물리는 지점:

| 지점 | 문제 |
|---|---|
| torchvision 자동 다운로드/압축 해제(`../../downloads/`의 MNIST·CIFAR) | 파일 수천 개 → 드라이브에 풀면 매우 느리고 쿼터 위험 (ch05·ch08) |
| HF 캐시를 드라이브로 돌릴 때 | blip2 15GB·Bllossom-3B를 드라이브에 받으면 저속·타임아웃·손상 위험 (ch12·ch13) |
| HF Trainer 체크포인트·`./qlora-funccall`·`./kobart-news`·회색조 PNG | 다수 파일 쓰기가 드라이브로 가면 매 저장이 느림 (ch12) |
| DataLoader가 드라이브 개별 파일을 매번 open | num_workers=0이라 FUSE 읽기 직렬화 → 처참한 속도 (도전 문제) |
| Drive I/O 쿼터 초과 | 소형 파일 폭주 시 마운트가 읽기전용/끊김 → 학습 중단 |

**결론**: "drive.mount + `%cd` 드라이브 폴더"는 `../../`를 맞춰 주지만 **모든 상대 읽기/쓰기가
드라이브로 향해 느려진다.** 따라서 **"`/content`에 git clone"(로컬 빠른 디스크)**이 성능상 정석이다.
드라이브는 **최종 산출물만 절대경로로 영속화**할 때만 사용:

```python
model.save_pretrained('/content/qlora-funccall/adapter')                 # 로컬(빠름)
!cp -r /content/qlora-funccall/adapter /content/drive/MyDrive/backup/    # 끝나고 한 번만
```
HF_HOME·downloads를 드라이브로 바꾸지 말 것(기본값 `/content`·`~/.cache` 유지).

## 7. 🟡 그 밖의 수정거리

- **`!pip install accelerate>=1.1.0`**(ch12-02, 2곳): 셸에서 `>`가 **리다이렉션**으로 해석돼
  `accelerate`만 설치되고 `=1.1.0` 쓰레기 파일 생성(버전 제약 무시). → `!pip install 'accelerate>=1.1.0'`.
- **한글 폰트 코랩 첫 실행**: `visualize.py`의 모든 plot 함수가 내부에서 `set_korean_plot_env()`를
  호출하지만, 코랩 첫 실행 시 `fonts-nanum` 설치 후 **"세션 다시 시작" 출력만 하고 그 세션엔
  폰트를 적용하지 않는다.** 첫 그림은 한글이 □로 깨지고 **재시작 후 재실행해야 정상**. 1회 안내 필요.
- **임시 저장 소실**: `./kobart-news`·`./qlora-funccall/adapter`·`checkpoints/`·회색조 `*.png`는
  CWD 상대 저장이라 코랩 재연결 시 사라짐(`.gitignore`로도 제외). 실행엔 무방, 보존 시 드라이브 안내.
- **대용량 다운로드**: blip2-opt-2.7b 약 15GB(13-02), Bllossom-3B(12장). 코랩 무료 디스크·시간 주의.
- **ch13-02 머라이언 이미지**: 노트북은 `requests.get(url)`(코랩 친화적), 본문 [코드 13-10]은
  로컬 파일(`Image.open(image_path)`)로 수정됨 → **노트북↔본문 불일치**(통일 여부 결정 필요).

---

## 8. 권장 공통 구성 (로컬·코랩 동일)

원칙: **첫 셀이 환경을 자동 감지** → 로컬이면 그대로, 코랩이면 `/content`에 clone 후 경로 정렬.
같은 셀이 양쪽에서 손 안 대고 동작(= "코랩에서 주석 풀기" 불필요 → README 불일치도 해소).
부트스트랩은 `code_reference` 임포트 *전*에 동작해야 하므로 self-contained여야 한다(닭-달걀).

### 옵션 1 — 최소 변경 (권장 시작점)
코랩에서 clone 후 챕터 폴더로 `%cd` → 양쪽 다 `CWD=notebooks/chNN` → 기존 `../../`·`../../data`를
**한 줄도 안 고쳐도** 됨. 노트북당 차이는 챕터명뿐.

```python
# === 공통 부트스트랩 (로컬·코랩 자동, 모든 노트북 첫 셀) ===
import os, sys
if 'google.colab' in sys.modules:                       # 코랩일 때만
    if not os.path.exists('/content/repo'):
        !git clone -q --depth 1 https://github.com/<org>/<repo>.git /content/repo
    %cd /content/repo/repo_root/notebooks/ch08          # ← 챕터만 교체
sys.path.append('../../')                               # 로컬·코랩 공통
from code_reference import common
from code_reference import visualize as viz
```
- 로컬: `if` 통째로 건너뜀 → 기존과 100% 동일.
- 코랩: clone(`/content`=로컬 빠른 디스크) + `%cd` → `../../`가 repo_root로 정확히 해석.
  `../../data`·`../../downloads`도 로컬을 가리켜 **드라이브 성능 문제 동시 해결**.
- 단점: `%cd`·`!git clone`은 IPython 매직(노트북 전용이라 무방).

### 옵션 2 — 더 견고 (절대경로, `../../` 제거)
`%cd` 대신 repo_root를 절대경로로 찾아 `sys.path`·데이터 경로 변수에 주입. CWD 무의존.
대신 노트북의 `../../data/...`를 `DATA/...`로 바꾸는 편집 필요.

```python
# === 공통 부트스트랩 (절대경로 버전) ===
import os, sys
if 'google.colab' in sys.modules:
    if not os.path.exists('/content/repo'):
        !git clone -q --depth 1 https://github.com/<org>/<repo>.git /content/repo
    ROOT = '/content/repo/repo_root'
else:                                                   # 로컬: code_reference 가진 상위 폴더 탐색
    ROOT = os.getcwd()
    while ROOT != os.path.dirname(ROOT) and not os.path.isdir(f'{ROOT}/code_reference'):
        ROOT = os.path.dirname(ROOT)
sys.path.insert(0, ROOT)
DATA      = os.path.join(ROOT, 'data')                 # os.path.join(DATA, 'cat.jpg')
DOWNLOADS = os.path.join(ROOT, 'downloads')            # torchvision root=DOWNLOADS
from code_reference import common
from code_reference import visualize as viz
```
- 장점: `../../` 의존이 사라져 어디서 열든(코랩 GitHub 직접 열기 포함) 무조건 동작, 챕터명 불필요.
- 단점: data/downloads 경로 라인을 일괄 치환해야 함.

### 어느 쪽?
- **빠른 일괄 적용** → 옵션 1(기존 `../../` 자산 유지 + 코랩 4줄).
- **장기적으로 깔끔(경로 취약성 제거)** → 옵션 2(경로 변수화 작업 포함).

---

## 9. 적용 체크리스트

- [ ] 표준 부트스트랩 셀(옵션 1 또는 2)을 **전 노트북 첫 셀에 통일** + README의 "복붙용 표준 셀"로 1벌 게시
- [ ] `get_device()` **XLA 분기 제거/경고** + README에 "TPU 런타임 금지(GPU/CPU만)"
- [ ] `!pip install 'accelerate>=1.1.0'` **따옴표**(ch12-02 2곳)
- [ ] clone/캐시/다운로드/출력은 **`/content` 로컬**, 드라이브는 최종 산출물 복사만
- [ ] 그림 들어가는 노트북에 **"코랩 첫 실행 시 폰트 설치 후 1회 재시작" 안내**
- [ ] num_workers=0 유지, 도전 문제에 "코랩 0~2" 주의
- [ ] (선택) ch13-02 이미지 로딩 방식 본문과 통일

---

## 10. ✅ 이미 잘 되어 있는 것

- `get_device()` cuda→mps→xla→cpu 자동, GPU 없으면 CPU 폴백.
- 코랩 한글 폰트 자동 설치(`check_colab` + `apt-get fonts-nanum`), `visualize.py`가 plot마다 폰트 적용.
- pip 설치는 장별 주석 안내, `requirements.txt`는 코어만 분리.
- ch12-04 GROQ 키 없을 때 **로컬 LLM 폴백**(`USE_LOCAL_LLM`).
- 전 노트북 `num_workers=0` → 코랩 워커/공유메모리 이슈 회피.
- `.gitignore`로 대용량 가중치·체크포인트·생성 PNG 제외.
- README에 "12-03 QLoRA는 GPU 런타임 필수" 등 GPU 안내 존재.
