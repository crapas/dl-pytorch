# 딥러닝 입문서 예제 코드 - 공통 의존성
#
# 본 파일은 CPU 빌드 PyTorch 를 기준으로 한다.
# CUDA 환경에서는 README 의 'CUDA 환경 설정' 절을 따라 별도 wheel 인덱스로
# torch / torchvision 을 재설치한다.
#
# 장 특화 라이브러리(transformers, peft, bitsandbytes, timm, kagglehub 등)는
# 각 노트북 첫 셀 주석에서 별도 안내한다 (자동 설치하지 않음).
#
# 출판 시점 검증 환경 기준:
#   Python 3.14, PyTorch 2.12.x, CUDA 13.2 (cu132 wheel)

# 핵심 (PyTorch)
torch~=2.12.0
torchvision~=0.27.0

# 수치·시각화
numpy>=2.0
matplotlib>=3.9

# 모델 요약
torchinfo>=1.8

# Jupyter 표시 위젯 (코랩·로컬 공용)
ipywidgets>=8.1
